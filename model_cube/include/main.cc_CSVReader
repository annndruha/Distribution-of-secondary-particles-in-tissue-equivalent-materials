#include "include/CSVReader.hh"

int main()
{
    CSVReader reader("../t1_statistic/results_nt_All particles_t0.csv");
    std::vector<std::vector<std::string> > dataList = reader.getData();
    for(std::vector<std::string> vec : dataList)
    {
        std::cout << vec[0];
        std::cout<<std::endl;
    }
    return 0;
}